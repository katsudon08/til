dockerでデータを保存しておく(コンテナが消えてもデータを永続化して置けるようにしとく)方式には2つやり方があって、

- named volume
- bind mount

の二種類があるみたい。
namedの方はdocker専用の入れ場所にデータを入れとく方式で、bindの方はプロジェクトフォルダの指定フォルダに入れとくやつらしい

---

印象に残ってることとして、責務の分離。
widgetで表示の責務に徹することで、利用側が細かい部分を管理する(操作する関数などはコールバックを引数で受け取っとくことで、その中身を知ることなく利用できる)
こういうものの積み重ねなんだろうな。